"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getBaseUrl = void 0;
var getBaseUrl = function (token) {
    var BASE_62_CHAR_SET = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz';
    var base62ToInt = function (e) {
        var t = 0;
        for (var n_1 = 0; n_1 < e.length; n_1++) {
            t = t * 62 + BASE_62_CHAR_SET.indexOf(e[n_1]);
        }
        return t;
    };
    var e = token;
    var t = e[0];
    var n = t === 'A' ? base62ToInt(e[1]) : base62ToInt(e.substring(1, 3));
    var i = e.indexOf(';');
    var r = e;
    var s = null;
    if (i >= 0) {
        s = e.slice(i + 1);
        r = r.replace(';' + s, '');
    }
    var serverPartition = n;
    var baseUrl = 'https://p';
    baseUrl += serverPartition < 10 ? '0' + serverPartition : serverPartition;
    baseUrl += '-sharedstreams.icloud.com';
    baseUrl += '/';
    baseUrl += token;
    baseUrl += '/sharedstreams/';
    return baseUrl;
};
exports.getBaseUrl = getBaseUrl;
