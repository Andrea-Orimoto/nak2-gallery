#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var _1 = require(".");
var token = process.argv.slice(2)[0];
if (!token) {
    console.error('Must provide a token as the first argument.');
    process.exit(1);
}
(0, _1.default)(token)
    .then(function (data) { return process.stdout.write(JSON.stringify(data)); })
    .catch(function (err) {
    console.error(String(err));
    process.exit(1);
});
