"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.enrichImagesWithUrls = void 0;
var enrichImagesWithUrls = function (apiResponse, urls) {
    var photos = Object.values(apiResponse.photos);
    var photosWithDerivativeUrls = photos.map(function (photo) {
        var derivativesObject = Object.values(photo.derivatives);
        var duplicateCount = [];
        var derivatives = derivativesObject.reduce(function (root, derivative) {
            var _a;
            if (typeof urls[derivative.checksum] === 'undefined') {
                return root;
            }
            var derivativeKey = derivative.height.toString();
            if (!!root[derivativeKey]) {
                duplicateCount.push(derivativeKey);
                var duplicateIndex = duplicateCount.filter(function (duplicate) { return duplicate === derivativeKey; }).length;
                derivativeKey = derivativeKey + '-' + duplicateIndex;
            }
            return __assign(__assign({}, root), (_a = {}, _a[derivativeKey] = __assign(__assign({}, derivative), { url: urls[derivative.checksum] }), _a));
        }, {});
        return __assign(__assign({}, photo), { derivatives: derivatives });
    });
    return photosWithDerivativeUrls;
};
exports.enrichImagesWithUrls = enrichImagesWithUrls;
